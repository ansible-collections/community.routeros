minor_changes:
  - api_modify - add ``comment`` attribute to ``/routing id``, make ``/ip traffic-flow target`` a multiple value attribute, add versioning to the ``/tool e-mail`` path (RouterOS 7.12 release), and add missing attributes to the ``routing bgp connection`` path (https://github.com/ansible-collections/community.routeros/pull/234).
